<style>
	#rebuild-button {
		position: absolute;
		right: 0;
	}
	#rebuild-status {
		padding: 1em 0;
	}
	#rebuild-form {
		position: relative;
		margin-top: 0;
		margin-bottom: 0;
	}
	#tagOptions {
		margin: 10px 0;
	}
	.tagOption {
		display: inline-block;
		margin: 0;
		padding: 0.5em 1em;
		background-color: #fff;
		border: solid 1px #93b874;
		font-weight: bold;
		font-size: smaller;
	}
	/*  */
	.tagOption:hover {
		text-decoration: none;
		background-color: #fafafa;
	}
	.tagOption-selected {
		background-color: #93b874;
		color: white;
	}
	.tagOption-selected:hover {
		background-color: #6D855E;
		border-color: #6D855E;
	}
	.tagOption:first-child {
		border-top-left-radius: 4px;
		border-bottom-left-radius: 4px;
	}
	.tagOption:last-child {
		border-top-right-radius: 4px;
		border-bottom-right-radius: 4px;
	}
	#results {
		border-collapse: separate;
		border-radius: 4px;
		overflow: hidden;
		border: solid 1px #ddd;
	}
	#results th {
		white-space: nowrap;
	}
	#results th,
	#results td {
		text-align: left;
		padding: 0.25em 0.5em;
	}
	.result:nth-child(even) {
		background-color: white;
	}
	.result:nth-child(odd) {
		background-color: #fafafa;
	}
	.result-ok {
		color: darkgreen;
	}
	.result-error {
		color: darkred;
		background-color: pink !important;
	}
</style>
<p>A list of YouTube IDs extracted from the Curatevideo templates in the video pages transcluded by articles with <a href="/Special:AdminTags">specific admin tags</a>.</p>
<form action="/Special:AdminYouTubeIds/{{tag}}" method="post" id="rebuild-form">
	<button type="submit" class="button" name="rebuild" value="1" id="rebuild-button">Rebuild list</button>
	<p id="rebuild-status">Last rebuilt on {{rebuilt}}</p>
</form>
<table id="results">
	<caption>
		<div id="tagOptions">{{#tagOptions}}<a href="/Special:AdminYouTubeIds/{{value}}" class="tagOption {{#selected}}tagOption-selected{{/selected}}">{{value}}</a>{{/tagOptions}}</div>
	</caption>
	<thead>
		<tr>
			<th>Page ID</th>
			<th>Page Title</th>
			<th>YouTube ID</th>
			<th>Channel</th>
			<th>Error</th>
		</tr>
	</thead>
	<tbody>
		{{#results}}
		<tr class="result result-{{status}}">
			<td>{{page_id}}</td>
			<td><a href="/{{page_title}}" target="_blank">{{page_title}}</td>
			<td><a href="https://www.youtube.com/watch/{{youtube_id}}" target="_blank">{{youtube_id}}</a></td>
			<td>{{channel}}</td>
			<td>{{error}}</td>
		</tr>
		{{/results}}
	</tbody>
</table>